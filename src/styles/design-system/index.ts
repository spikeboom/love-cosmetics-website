/**
 * Lové Cosméticos - Design System
 *
 * Este módulo exporta todos os tokens e constantes do design system.
 * Importe daqui para usar os valores consistentes em todo o projeto.
 *
 * Uso:
 * import { colors, typography, shadows } from '@/styles/design-system';
 *
 * PARA O CLAUDE:
 * Antes de criar novos componentes, leia:
 * - src/styles/design-system/patterns.md (padrões e convenções)
 * - src/styles/design-system/tokens.ts (valores exatos)
 * - src/styles/design-system/examples/*.tsx (exemplos de referência)
 */

export {
  colors,
  typography,
  shadows,
  borderRadius,
  spacing,
  breakpoints,
  transitions,
  components,
} from './tokens';
